/*module.exports = function(sequelize, DataTypes) {
    var User = sequelize.define('User', {
      id: {
        type: DataTypes.STRING,
        allowNull: false,
        unique: true,
      },
      reminders: {
        type: DataTypes.STRING,
        allowNull: false,
        
      },
      data: {
        data: DataTypes
      }
    });
    
    return User;
  };*/
  